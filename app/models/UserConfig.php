<?php

class UserConfig extends User {
	// TODO - Make this class singleton.
	// Think a little bit.
	// May be UserConfig::write('weather', data) will be better.
	// UserConfig::read('weather')
	// UserConfig::default('sliderSettings')
	// UserConfig::write('sliderConfig', data)
	// UserConfig::read();
}